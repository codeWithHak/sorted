# Docker Compose environment variables
# Copy to .env and fill in your values: cp .env.example .env

# === Backend (FastAPI) ===
# Neon asyncpg format
DATABASE_URL=postgresql+asyncpg://user:password@ep-xxx-pooler.us-east-1.aws.neon.tech/sorted?sslmode=verify-full
OPENAI_API_KEY=sk-...
# CORS_ORIGINS is set in docker-compose.yml (defaults to http://localhost:3000)

# === Frontend (Next.js) ===
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=your-secret-here-min-32-chars
BETTER_AUTH_URL=http://localhost:3000
# Neon standard pg format (NOT asyncpg)
AUTH_DATABASE_URL=postgresql://user:password@ep-xxx-pooler.us-east-1.aws.neon.tech/sorted?sslmode=require
# Used at build time for client-side API calls
NEXT_PUBLIC_API_URL=http://localhost:8000

# === Local Postgres (docker-compose db service) ===
POSTGRES_PASSWORD=postgres
