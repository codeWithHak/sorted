# Database Configuration
# Use the pooled connection endpoint (with -pooler suffix) for Neon serverless
# Format: postgresql+asyncpg://user:password@host/dbname?sslmode=verify-full
DATABASE_URL=postgresql+asyncpg://neon_user:neon_password@ep-xxxxxxxx-pooler.us-east-1.aws.neon.tech/sorted?sslmode=verify-full

# For local development with PostgreSQL:
# DATABASE_URL=postgresql+asyncpg://postgres:password@localhost:5432/sorted

# Authentication
# JWKS endpoint URL (Better Auth's public key endpoint)
# In deployment: set to your deployed frontend URL, e.g. https://sorted.vercel.app/api/auth/jwks
JWKS_URL=http://localhost:3000/api/auth/jwks

# CORS allowed origins (comma-separated for multiple origins)
# In deployment: set to your deployed frontend URL, e.g. https://sorted.vercel.app
CORS_ORIGINS=http://localhost:3000

# OpenAI API key (for AI chat agent)
OPENAI_API_KEY=sk-...
